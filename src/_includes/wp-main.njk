---
layout: wp-base.njk
---
<section class="bg-white mx-auto">
<div class="container px-[5%] rounded-3xl py-16 grid gap-8  text-gray-600 pt-16">
    <div class="grid ">

        <img class="mb-4 " src="{{image}}" alt="{{image-alt}}">
        <p class="pr-8 text-2xl font-light">{{description2}}</p>
        <p class="text-2xl text-sky-500 my-8">Tags: {{type}}</p>
    </div>
    <div class=" text-2xl grid grid-cols-2 gap-16">
        <div>
        <p class=" text-4xl font-bold text-sky-500">{{title}}</p>
        <p class="py-2 mb-8">{{description}}</p>
        <a href="{{link}}"
                class="border border-sky-500 text-sky-500 px-16 py-1 rounded-full hover:text-zinc-800 hover:bg-sky-500 duration-500 transition ease-in-out">Visit
                Website</a>
    </div>
    <div class="">
        <p class='text-5xl '>Download link</p>
        <p class='text-xl pt-10'>{{links}}</p>
    </div>
    </div>
   
</div>
</section>